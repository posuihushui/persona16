# Agents

本文件记录 Persona16 的长期原创原则、产品判断和 agent 协作约定。后续迭代如果改变核心设计，应同步更新本文件和 CHANGELOG.md。

## 产品原点

Persona16 是面向微信和抖音用户的轻量自我认知测试站。第一款产品是「16 型人格测试」。它的价值不是给用户贴一个终身标签，而是用一次 8 分钟的结构化自评，换一份用户愿意截图、愿意转发、愿意付费读完的自我描述。

用户来自社交流量，不是搜索流量。所以产品的第一性问题不是「测得准不准」，而是「结果页值不值得发到朋友圈」。传播力决定获客成本，报告质量决定付费率，两者都不能牺牲。

## 原创产品原则

1. 分享不锁内容，只影响价格：不做「分享给 3 个好友才能看结果」这类内容门槛。免费能看到的东西，在分享之前和之后完全一样；分享换到的是深度报告的折扣，不是任何一段被藏起来的文字。这条是底线，越过它既违反微信规则也会伤口碑。
2. 付费卖深度，不卖结果本身：类型码、四条维度的位置、人设标签和关键词永远免费，性格描述给出开头一段。付费解锁的是完整描述和深度报告。用户付钱前必须已经知道自己是什么类型，落在四条轴的哪里。
3. 不做人格宿命论：所有文案必须表述为倾向和偏好，不能表述为能力上限、智商、命运或不可改变的属性。禁止出现「你注定」「你永远不可能」这类表达。
4. 不冒充心理诊断：本站是自我认知娱乐工具，不是临床量表。任何页面不得出现诊断、治疗、疾病名称或疗效暗示。涉及抑郁、焦虑、自伤等表达的用户输入，结果页必须给出求助资源而不是继续卖报告。
5. 不使用 MBTI 商标和受版权保护的题库：产品名为「16 型人格测试」，题目全部自建。四维度八极的理论框架属于公共知识可以使用，Myers-Briggs 的商标、官方题目文本、官方类型描述文本一律不得出现在代码、内容包和运营物料中。
6. 测试是内容，不是代码：新增一款测试必须只写内容包和文案，不改引擎。任何需要改 `src/lib/scoring.ts` 才能上线的新测试，说明内容包 schema 设计错了，先改 schema。
7. 结果可复现且可追溯：同一份作答在同一内容包版本下必须得到同一结果。内容包一旦发布就冻结，修订题目必须升版本号，历史结果永远按当时版本渲染。
8. 匿名优先：完成测试不需要注册。仅在付费和跨设备找回报告时才需要身份，且优先使用微信 openid，不主动索取手机号。
9. 付费必须可找回：用户付费后换了设备、清了缓存、重新进入，必须能凭微信身份或订单查询码找回报告。付费内容丢失是信任事故，不是客服问题。
10. 支付以微信生态为准：微信内用 JSAPI，微信外浏览器用 H5 支付，抖音内置浏览器不具备唤起条件时必须显式引导而不是静默失败。支付能力通过 provider 接口接入，本地可以 mock，生产必须真实预下单、验签、幂等，并在支付回调确认后再发放权益。
11. 域名和落地页必须假设会被拦截：微信对测试类 H5 的拦截是常态而非意外。分享链路必须支持域名池切换、被拦截时的兜底引导页，以及不依赖单一域名的报告找回方式。
12. 运营不碰敏感个人信息：不在测试中收集身份证、手机号、住址、生物特征、精确定位。生日只收集到年月且仅用于分组统计，用户可以跳过。
13. 数据留存以统计为目的：作答明细用于计分和报告复现，聚合后用于优化题目。不做用户画像外售，不与第三方广告平台共享作答明细。
14. 文案由内容包驱动，不散落在组件里：所有面向用户的测试文案必须来自 `content/`，组件里只允许出现按钮、导航这类结构性 UI 文本。
15. 免费部分要给到真东西：类型码和四条轴的位置是完整的、准确的、不打折的，用户不付钱也能拿到一个确切的答案。被截断的是描述的深度，不是结论本身。付费点出现在用户已经知道自己是谁之后。
16. 内容页可索引，个人页不可索引：类型解读页是搜索和 AI 的入口，必须可抓取；用户的个人结果页是个人数据，对所有抓取器一律禁止，canonical 指向对应的类型页。
17. 对 AI 抓取开放的是内容不是数据：公开的原创描述欢迎 AI 引用，llms.txt 里写明引用要求。付费报告和用户作答不进任何对外文件。
18. 回调不是唯一路径，但服务端是：权益只能由服务端发放，回调和主动查单都算；前端的支付成功回调只用来触发确认，永远不作为发放依据。
19. 兼容按国产内核算，不按 Chrome 算：用户在微信 X5、MIUI、华为、UC、QQ 浏览器里，兼容基线是 Chrome 70 / iOS 12。抬高基线要有理由，不能因为写起来方便。
20. 不引用任何境外资源：字体、统计、依赖、图片全部本地化。国内加载不出来的东西等于不存在。
21. 图形优先于文字：用户不读任何一段完整文字，也应该知道这一页在讲什么、下一步点哪里。能画的不写，长文本折叠但保留在 HTML 里。
22. 图形必须携带信息：所有图形从内容包数据生成，不做纯装饰的插画。改内容时图形自动跟着变，不会对不上。
23. 价格只在服务端算：折扣力度、有效期、最终金额全部由服务端按内容包配置决定，客户端既不发送价格也不发送折扣码。下单时重新算一遍，不复用展示过的报价。
24. 付费正文不进任何公开出口：页面 HTML、meta、结构化数据、llms.txt、接口响应，一处都不能整段泄漏。前端不做隐藏式伪装，截断在服务端完成。
25. 主视觉是产品资产：16 张类型主视觉由类型码生成，色板柔和、同一套设计语言。它会被截图外发，所以颜色不跟随深浅色模式，且结果页、类型页、分享卡必须共用同一份几何。

## 技术方向

- 第一阶段载体：H5 网页，移动端优先，主要在微信内置浏览器和抖音内置浏览器打开。不做小程序。
- 前端与后端：Next.js App Router 单体，TypeScript。页面用 Server Component 渲染，交互用 Client Component。API 走 Route Handler。
- 数据库：MySQL + Prisma。部署在国内服务器，域名已备案。
- 内容包：`content/tests/<slug>/` 下的版本化 JSON，构建期加载，`npm run check:content` 是提交门槛。
- 计分引擎：`src/lib/scoring.ts` 与具体测试无关，只消费内容包声明的维度、权重和类型映射规则。
- 支付：`src/lib/pay/provider.ts` 定义统一接口，`mock` 用于本地，`wechat` 用于生产。新增支付方式只实现 provider，不改业务代码。接口包含预下单、回调验签、主动查单、关单和退款五件事，缺一不可。
- SEO 与 AI：sitemap、robots、llms.txt、结构化数据全部由内容包生成，不手写。类型解读页 `/t/<slug>/type/<code>` 是搜索和 AI 的主要入口。
- 样式层不使用 CSS 框架：兼容基线是 Chrome 70 / iOS 12，主流框架的产物会超出这个水位。flex 的 gap、逻辑属性这类要带回退，规则见 `docs/operations/china-devices.md`。
- 分享卡片：结果页 OG 图由 `/api/og` 动态生成，包含类型码和一句人设标签，不含用户任何个人信息。
- 部署产物：`output: "standalone"`,容器化上线。

## 数据与权限约定

- `sessionKey` 是匿名用户标识，写入 HttpOnly Cookie，用于把作答和订单归到同一浏览器。
- `openId` 是微信身份，仅在支付或跨设备找回时通过网页授权获取，静默授权只取 openid 不取用户信息。
- `Attempt` 是一次作答，包含答案明细、内容包版本和计算出的类型码。作答归属 `sessionKey`,支付后额外绑定 `openId`。
- `Order` 是一次付费，权益粒度是「某个 Attempt 的深度报告」,不是账号级订阅。
- 深度报告接口必须服务端鉴权：请求方需要持有该 Attempt 的 `sessionKey`,或持有绑定同一 `openId` 的有效订单。前端不得凭 URL 直接拿到付费内容。
- 支付回调必须验签、幂等，并且以回调为准发放权益。前端支付成功回调只用于刷新界面，不作为发放依据。
- 微信 AppSecret、商户私钥、APIv3 key 只允许服务端读取，不得进入客户端 bundle 或内容包。
- 作答明细保留用于报告复现;用于题目优化的分析必须使用聚合数据。
- 用户可凭订单查询码请求删除自己的作答和订单，删除后保留必要的支付审计线索。

## Agent 协作约定

本项目按角色拆分 agent,定义在 `.claude/agents/`。每个 agent 只在自己的目录边界内写文件，跨边界的改动必须先提出再交给对应 agent。

| Agent | 职责 | 主要产出目录 |
| --- | --- | --- |
| `product-owner` | 需求拆解、PRD、付费点设计、指标定义 | `docs/product/` |
| `test-designer` | 题目设计、维度权重、计分规则、信效度自查 | `content/tests/*/questions.json`、`scoring.json` |
| `content-writer` | 类型描述、深度报告、结果页文案 | `content/tests/*/results/` |
| `visual-designer` | 视觉语言、答题与结果页设计、分享卡样式 | `docs/design/`、`src/app/globals.css` |
| `frontend-engineer` | 页面与组件实现 | `src/app/`、`src/components/` |
| `backend-engineer` | 数据模型、接口、支付、鉴权 | `src/lib/`、`prisma/`、`src/app/api/` |
| `growth-operator` | 传播机制、投放、私域、防封策略 | `docs/operations/` |
| `compliance-reviewer` | 商标、广告法、心理表达边界、个人信息合规 | `docs/legal/`,对全仓有否决权 |

协作规则：

- 新增一款测试走 `/new-test`,顺序是 product-owner → test-designer → content-writer → compliance-reviewer,四步都通过才进入前端联调。
- `compliance-reviewer` 对任何面向用户的文案有一票否决权，它的意见不能被产品或运营理由覆盖。
- 任何 agent 发现原则冲突时，停下来在本文件提出修订，不要在代码里绕过原则。
- 每个 agent 交付前必须自己跑对应的检查脚本，不把失败的产物交给下一个 agent。

## 迭代要求

- 每次重要变更更新 CHANGELOG.md。
- 产品和架构变更写入 `docs/`。
- 改变核心原则时更新本文件。
- 新增或修改内容包后，必须运行 `npm run check:content`。
- 新增或修改面向用户文案后，必须运行 `npm run check:compliance`。
- 涉及生产部署或生产环境变量调整时，必须运行 `npm run check:prod-env -- --env-file <生产环境变量文件>`。
- 涉及支付、鉴权或权益发放时，同步检查 `docs/product/prd-v1.md` 的权益边界描述是否仍然成立，并查阅 `docs/operations/wechat-pay.md`。
- 涉及微信支付密钥、证书、平台公钥或支付部署时，必须运行 `npm run check:pay-rotation -- --env-file <生产环境变量文件>`，并查阅 `docs/operations/wechat-pay-key-rotation.md`。
- 涉及页面结构、标题、内链或结构化数据时，先查阅 `docs/operations/seo.md`，并确认个人结果页仍然是 noindex。
- 涉及样式、依赖或任何第三方 SDK 时，必须运行 `npm run check:china`，并查阅 `docs/operations/china-devices.md`。
- 改动图标或品牌色后，重新运行 `npm run build:icons` 并提交生成的 PNG。
- 涉及分享、投放、社群或话术时，先查阅 `docs/operations/wechat-distribution.md`,并检查诱导分享、广告法和心理表达边界。
- 上线前运行 `npm run ship`。
