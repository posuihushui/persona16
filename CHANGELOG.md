# Changelog

## 未发布 — 2026-09-09（一）

类型主视觉从抽象几何换成 16 只原创类型动物。

- 每个类型一只动物，侧身站在同一条地面线上，手里拿着一件对应它类型标签的道具：内燃灯提灯、点灯人挑长杆灯、远眺者架望远镜、思维矿工戴矿灯拿镐、拆解手拿扳手、织网人抱线团。改类型标签时要回头看这只动物还对不对。
- 换掉抽象几何的原因是小尺寸认不出来：上一版四个字母各控制主形的一个属性，在海报上成立，但矩阵页缩到 44px、配对卡缩到 40px 时差异全挤在「方还是圆、大还是小」上，同色系四张几乎一样。剪影换成长耳、长颈、壳、鳍之后小尺寸依然分得开。
- 动物与道具全部原创，不沿用任何第三方已有的类型角色设定。
- 画面仍由 `typeArtMarkup()` 产出字符串，页面、海报和 `/api/og` 分享卡共用同一份，`toneFor()` 的色板与调用方一处没动，类型卡色条和关键词瓦片照旧。
- 道具色固定成一个暖金，不跟色系走。按色相取补色试过一版，藕粉那组补出来是草绿，灯和橡果全成了绿的，和身体颜色直接打架。
- 四肢改成「先描边再填色」两遍画法（`limbs()`）。用分开的矩形拼腿试过一版，四条腿会各自飘在身子外面接不上。
- `TypeArt` 在 80px 以下换用更紧的视框，把角色四周那圈纸裁掉，小尺寸下角色更大。改姿势时别让道具越过这个框。

`npm run ship` 通过。16 只在 400px、96px、56px、40px 四档下逐一看过，类型页、类型目录页和 `/api/og` 分享卡在浏览器里确认过。

## 未发布 — 2026-09-08（三）

把分享卡做厚：一张卡上同时给出信条、类型码、四条轴和口语标签。

- 合规脚本补上第三方产品对 16 个类型的中英文命名。这些词单独出现大多是普通词，所以只在它紧挨着类型码或「人格 / Personality」时才判定为沿用，避免在架构文档里大面积误报。
- 结果页海报和 `/api/og` 分享卡的版面收敛到 `src/lib/poster.ts` 一份模型：卡上有哪些块、什么顺序、文字怎么断行、色条填到哪、印章和走马带写什么都由它算，两个渲染器只负责画，印章和走马带直接共用同一段标记。
- 两张卡分工写清楚：结果页那张印本人的四条轴，是用户自己截图用的；公开分享卡会被转发，不含任何一次作答的数据。
- 海报新增信条、口语标签、骑缝章和卡底走马带；卡底那行四维文字摘要换成四条色条，从中线往命中那一端填，两端同色。
- 内容包新增两个免费字段 `creed` 与 `tags`，16 个类型各写了一句信条和六条口语标签；长度与条数进校验，排版不变量进测试。
- 品牌金只用在骑缝章上：类型色板继续压在低饱和，16 张卡共用一个亮色，比 16 套吵闹配色更成套。
- 修掉海报小字的对比度问题：套印色的明度随色系在 38% 到 70% 之间浮动，16 个类型里有 10 个拿它当 11px 小字达不到 4.5:1，最差的只有 2.2:1。小字统一改用明度固定的 `tone.ink`，并去掉承载信息的小字上的透明度——最差那组本来就没有余量。新增测试对 16 套色板逐一验证。
- 主视觉自带一张不透明纸色底，贴在海报的类型色底上会留一道接缝，两个渲染器都给它加了圆角和一圈细边，让它成为一块有意为之的画板。
- 测试脚本改为带 `--import ./scripts/register-ts.mjs` 运行，源码里不带扩展名的相对导入才解析得了。

`npm run ship` 与本地验收脚本通过。分享卡的排版不变量对 16 个类型逐一验证：标签不压走马带、不出画布、标签过长时自动减条数。

## 未发布 — 2026-09-08（二）

全站减少描述性长文，把内容按它自己的形状排成卡片、阶梯、瓦片和对照栏。

- 类型页六节不再是同一个模子：版式按这一节有什么自己选（对照栏不出图、并列短句用铺满头图、其余左右交替），那句结论改成引用卡，关键词从一排 chip 换成瓦片，相关类型带上主视觉，并补回四条轴的示意位置。
- 结果页未付费时不再只剩一张卡和 62 字预览：把免费的长处 / 难处对照栏搬上来，再给出公开解读其余几节的入口，链到类型页对应锚点，不复制正文。
- 付费墙十行带锁文字改成四张卡的目录网格，其余六项收进 `details`；上方新增报告版式骨架，只有灰条，不含任何正文。
- 报告页重排：新增封面（紧凑海报加四条轴）与吸顶目录；认知偏好排成顺位阶梯，优势与成长建议变成结论加粗的编号卡，盲点改成「看到的 / 这周做的」双格卡并可勾选（只存本机，`localStorage` 读写包 try/catch），相处对象改成双主视觉的配对卡，岗位方向改成瓦片。
- 首页补三步流程、四条轴的示意光谱和 16 张主视觉横条；介绍页补三个数字的速览条和「先试一题」——一道真题加真实五档量表，不计分、不存进度、选完只给一句说明。
- 类型目录页的 16 行同形列表改成按精力与节奏分成四组的卡片，加字母筛选；筛选只改已渲染节点的属性，没有 JS 时 16 张一张不少。
- 报告页原来写死在组件里的小节标题全部移进 `content/ui.json`。
- 结论与解释的拆分由文本自身决定（`src/lib/prose.ts` 按第一个逗号或句号切，带长度闸门，切不动就整条显示），所以这轮排版没有要求内容包重写任何一份结果文件。
- 内容包新增可选的 `guide.layout`，用于打断版式轮换；校验脚本会拦下非法取值。规范同步说明默认的版式派生规则。
- 修复类型卡与关键词瓦片在深色模式下浅底配浅字的对比度问题：类型色板只做填充（左侧色条与标记），底色和文字一律走主题令牌。
- 本地验收脚本的付费边界断言从整串比对改成逐段比对，认知正文拆层渲染之后仍然严格——公开出口漏掉任何一段都会被拦下。

`npm run ship` 与本地验收脚本通过；已在 320 / 390 / 768px 视口和深色模式下检查新组件的布局、对比度与横向溢出，并实际走通试答一题、字母筛选、勾选动作与跨刷新恢复。

## 未发布 — 2026-09-08

类型解读页改成一篇能读完的公开解读，插画换成矢量并加入轻微动效。

- 每个类型新增免费的 `guide` 公开解读，分「开场 / 长处与难处 / 恋爱 / 友谊 / 工作 / 写在最后」六节，用日常说法写这一类人的共性；付费报告继续只写「你自己」，校验脚本会拦截两者的整段重复。
- 类型页按章节重排：吸顶目录加阅读进度、每节图文头、两栏长处难处、下一节入口；960px 以上换成左目录右正文的两栏，1280px 封顶，320px 无横向溢出。
- 情境插画改为内联 SVG，新增「恋爱」「工作」两个场景，单张体积从 100KB 级降到 2KB 级；每张图有一两处 2–6px 的轻微动作，`prefers-reduced-motion` 时全部静止。
- 插画放开单色限制：六个色系各管一张图，人物的衣服、头发、肤色和道具都上色。同一张图里的两个人保持同样鲜亮，不用颜色暗示哪种偏好更好。
- 插画整体放大一档，类型页的章节头图改成铺满正文宽度的 8:5 横幅，两侧补配景；其余位置的图从 104px 提到 120px 上下。
- 动效加大到一眼能看见：人物起伏并眨眼，挥手的胳膊绕肩摆动，太阳转、云飘、心跳、水滴落下、看板柱子逐根升起；一节进入视口时图、标题、正文依次进场，「下一节」的箭头持续轻推。
- 进入视口淡入默认可见，脚本起来后才隐藏视口外的块，禁用 JS 和抓取器看到的仍是完整内容。
- 修复介绍页封面在 640px 以上被挤成窄列、标题断行的问题。
- 内容包规范补上 `guide` 的字段、写法与校验规则；只增加公开解读、不动题目与计分时不升版本号。

`npm run ship` 通过；已在 320 / 375px 手机视口和 1280px 桌面检查布局、目录高亮与动效。

## 未发布 — 2026-09-07

扩展青绿色人物情境插画，让公开内容与报告章节更容易浏览。

- 新增自我探索、精力、信息、决策、节奏、成长、沟通 7 幅原创场景，统一采用细线、柔和青绿和纸张颗粒；本地提供 WebP 与 PNG 回退。
- 首页和测试介绍加入主插画、场景卡及四维生活图解；类型目录、类型解读、结果描述与报告章节增加对应插画。
- 答题页仅展示不随题目变化的中性小图，短屏隐藏；为图片预留空间，非首屏图延迟加载。
- 资产、替代文本、新文案与位置统一由 `content/illustrations.json` 管理，生成提示词和维护规范在 `docs/design/` 留档。
- 同步图形原则：允许解释公开主题的原创情境插画，真实得分轴和类型主视觉仍由数据生成。

`npm run ship` 与 18 项自动测试通过；已在 320/375px 手机视口和桌面检查布局，并实际走通 48 题、本地模拟支付及报告阅读。素材均在本地加载，7 张 WebP 合计约 802 KB。验证范围见 `docs/design/illustrations.md`。

## 0.8.0 — 2026-09-07

按结果页、分享、答题和完整链路的优先级完成一轮移动端优化。

- 结果卡改为紧凑海报，手机首屏呈现类型、标签、关键词和保存 / 复制入口；四维轴显示真实的两极位置，公开类型页明确标注示意位置。
- 增加 900×1200 PNG 分享卡，沿用类型主视觉几何和固定色板；按需生成、失败可重试，微信和抖音各有保存提示。
- 分享链接统一指向公开类型页，不带个人结果、作答和找回码；自动复制失败时回退传统复制，再提供手动复制。
- 付费清单先展示三项，其余可展开；领取优惠与下单增加超时和重复点击保护，确认支付后进入报告，补上找回入口与跨设备微信身份判断。
- 答题增加继续 / 重新开始、损坏缓存修复、不能保存时提示、提交超时重试和题库版本校验；保持即时切题和减少动画偏好。
- 历史结果、报告、报价和卡片按作答版本加载；缺少历史内容时不回退到新版本。内容校验覆盖版本归档。
- 修复 flex 间距的特性检测误判，始终使用兼容外边距；同步纠正国内浏览器检查脚本。
- 新增进度恢复、内容版本、复制兼容测试，以及仅限本地 mock 环境运行的完整链路验收脚本。

`npm run ship`、18 项自动测试、本地模拟支付完整链路及构建后的国内兼容检查通过。验收范围与上线前尚需完成的真机验证见 `docs/product/optimization-2026-09-07.md`。


## 0.7.0 — 2026-09-06

答题界面重做。同一个界面要被看 48 次，任何多余都会被放大 48 倍。

### 简洁

- 去掉重复的计数器，进度条和「12 / 48」合成一行。
- 去掉「下一题」按钮，它和选完自动进入重复。
- 底部提示只在第一题出现，之后用户已经知道怎么答了。
- 「上一题」从按钮降级成一行淡色文字，第一题不显示。

### 丝滑

- **切题改为即时**，实测点击到新题出现 5 到 8 毫秒。原先做的是「淡出 170ms 再淡入」的两段式，两个问题：48 题累计白白多花 8 秒；而且它依赖定时器，页面被挂起时浏览器会把定时器节流到一秒开外，用户点了没反应，实测后台标签页里就是 1000 毫秒。
- 顺滑改由入场动画承担：新题从侧面滑入并淡入，240ms 缓出，回退时方向相反。动画不挡操作，可以在播放期间直接点下一题。
- 题干区固定成正好两行的高度。实测 48 题里选项顶部始终在同一个像素上，不再有 2px 的抖动。
- 动画只用 transform 和 opacity，走合成层，不触发重排。
- 进度恢复改到 `useLayoutEffect`，在绘制之前完成。首屏不再先渲染一个空占位框再出题，SSR 直接带出第一题和五个选项。
- 补上 `prefers-reduced-motion`：开了这个偏好的用户直接切，不做位移。

### 验证

- 快速连点四次只前进一题，不会跳题。
- 答完最后一题但前面有空时，跳回第一个未答题而不是提交。
- 48 题走完提交并跳转结果页，本地进度清空。

## 0.6.0 — 2026-09-06

付费墙从「读完免费部分再决定买不买」改成「先给结论，深度收费」，并加了分享换折扣的裂变机制。

**这一版推翻了 0.1.0 定下的两条原则**：免费结果不再完整可读，且引入了利益诱导分享。原则 1、2、15 已相应改写，不留自相矛盾的文档。

### 内容边界

- 免费保留结论：类型码、类型名、人设标签、三个关键词、四条轴上的位置与强度。这些是完整的、准确的、不打折的。
- 性格描述只给开头 62 字并渐隐，其余连同最舒服 / 最容易累的状态一起进入付费。
- 截断在服务端完成。付费正文不进页面 HTML、不进 meta、不进结构化数据、不进 llms.txt、不进接口响应。前端不做隐藏式伪装。
- 预览长度有校验上限：不得超过最短正文的六成。这是硬拦截。设成 120 字时脚本会明确告诉你最短的 core 是 INTJ 的 137 字，上限是 82 字。

### 付款界面

- 新增 `Checkout`：锁定清单十项各带标题和钩子、价格与划线价、折扣标记、倒计时、裂变入口、支付按钮、退款说明。
- 新增 `LockedPreview`：截断正文加渐隐，并告诉用户还剩多少字。

### 分享折扣

- 分享结果可领 24 小时 6 折券，折后 5.94 元。
- **价格全部在服务端算**。客户端既不发送价格也不发送折扣码，下单时重新算一遍，不复用展示过的报价。实测注入 `amount: 1` 后订单仍是 990 分。
- 券绑定领取时的会话，别人拿到结果页链接领不了（403），也用不了。
- 一次作答一张券，唯一索引兜底，反复点分享不刷新有效期。
- 券在支付成功时核销，回调和主动查单两条路径都做了。过期后自动回到原价。
- `discount.trigger` 可切 `timed`：变成纯限时促销不绑定任何行为，前端自动隐藏分享入口，服务端改为首次看到结果时自动发券，平台风险归零，不用改代码。

### 合规

- 新增「利益诱导分享」建议项，每次检查都会输出。它不阻断，因为这是产品决策；但必须长期可见，不要消音。
- 分享解锁内容仍然是阻断项，这条线没变。
- 类型页的结构化数据改标 `isAccessibleForFree: false` 并用 `hasPart` 标出付费区块。页面部分付费却声明成全免费会被判 cloaking。

### 修复

- 类型页的 meta description 和 Article 结构化数据原本直接切 `core` 的前 90 和前 160 字，而最短的 core 只有 137 字，等于把付费正文从结构化数据漏出去。改为一律走免费预览。
- 公开的类型页原本展示完整正文和最舒服 / 最容易累的状态，等于绕开付费墙。改为与结果页同一套截断规则。
- `llms-full.txt` 原本输出付费正文全文，改为只输出预览。

## 0.5.0 — 2026-09-06

补上真正的视觉。上一版把文字整理成了信息图，但那是排版不是设计，站上没有一张像样的图。

### 类型主视觉

- 16 张大幅几何构成，由类型码生成，同一套设计语言。一个主形、一条切过它的带、
  两者交叠处套印出第三个颜色，形少色少，靠构成说话。
- 四个字母各控制一件事：E/I 管主形大小与出血，S/N 管方还是圆，
  T/F 管切带硬边还是圆头弧，J/P 管正交还是斜切。第一版让 S/N 同时管两件事，
  结果同色系里四张几乎撞脸，差异都挤在一个维度上。
- 色板全部压到 36% 饱和度以下。按 SN × TF 分四个色系共享色相：
  雾蓝、藕粉、鼠尾草、陶土，组内再按 E/I 与 J/P 分四档。
- 几何以字符串形式产出，React 组件与 `/api/og` 分享卡共用同一份，
  两边不会画出两套图。
- 新增落地页封面 `CoverArt`，四个色系的形叠在一起套印，意思是四条轴组合出很多种。

### 海报式头图

- 结果页与类型页换成 `TypePoster`：图占满卡片上半部，底色来自类型自己的色板。
- 文字做真正的层次：顶部小字拉到 0.24em 字距当标签，类型码 3.75rem 并把字距
  收到 -0.035em、行高压到 0.92，中间一条短线断开，描述用更松的行高。
- 海报的颜色不跟随主题令牌。它会被截图外发，深浅色模式下必须长得一样。
- 分享卡改用同一套几何与排版，900×1200。
- 矩阵与索引列表的缩略图从四轨指纹换成小幅主视觉。

### 修复与清理

- 陶土色系压深后发褐：套印色改为只压明度不转色相，转色相会让暖色整体发脏。
- 类型页的 h1 与海报重复，并入正文标题。
- 删除被主视觉取代的 `TypeMark` 与 `.hero` 系列样式。
- 修复 `.chip` 基础样式随 `.hero` 一起被误删的问题。
- `/api/og` 原本想用 `react-dom/server` 复用组件，Next 在 app 目录里禁止这么做，
  改成几何产出字符串，两边共用。
- 恢复被 `pnpm remove` 连带清掉的 `@prisma/adapter-mariadb` 实体。

## 0.4.0 — 2026-09-05

排版从文字优先改成图形优先。测试类产品的用户不会读完长段落，之前的落地页是一堵文字墙。

### 图形组件

- `ScoreSpectrum` 维度光谱取代原来的单向进度条。进度条会被读成「你得了多少分」，但这四个维度不是打分，是落在两极之间的位置。两端都标出来之后，用户一眼就明白这不是考试成绩。
- `TypeSpectrum` 与 `DimensionAxes`：类型页展示该类型的典型位置，落地页展示只有刻度没有得分点的轴。
- `TypeMark` 类型指纹：四条轨道四个点，从类型码生成，用在矩阵和列表里做视觉区分。
- `TypeGrid` 16 型矩阵取代扁平的标签列表。行列各自有含义，用户可以先定位自己在哪一行再在四个里挑。行列划分从内容包推导，任何 dichotomy 内容包都适用。
- `Icon` 与 `SectionHead`：11 个线条几何图标，全部内联 SVG，用 currentColor 描边，深浅色自动跟随，不加载字体图标也不加载图片。
- `.versus` 两栏对比、`.facts` 数字速览、`.numbered` 编号卡三种版式。

### 页面重排

- **落地页**：首屏压缩到标题、三个数字卡和开始按钮，用户不读任何段落就能开始。往下是图形化的四条轴和 16 型矩阵。折叠之前的文字从 1300 字降到 583 字，长文放进 details，内容仍在 HTML 里供抓取。
- **结果页**：维度改用光谱，最舒服和最容易累的状态改成蓝橙两栏对比。
- **类型页**：四个字母的文字列表改成光谱图，头图加类型指纹，状态对比同上。
- **类型索引页**：矩阵在前，下面是一行一句的紧凑列表。
- **报告页**：七个区块各加图标锚点，工作环境改成两栏对比，成长建议改成编号卡。
- **答题页**：五级量表用圆点大小和颜色表达强度，底部提示从两句压到一句，进度区增加剩余题数。

### 修复

- 维度正好落在中点时，光谱高亮的一极与类型码取的一极会不一致。改为以计分引擎给出的极为准。
- 轴的两端原本用蓝橙渐变，而站内橙色代表「消耗」，会暗示某一极更差。改成两端同色，四条轴的两极没有好坏之分。

## 0.3.0 — 2026-09-05

面向国内真实使用环境的适配。用户在微信 X5、MIUI、华为、UC、QQ 浏览器里，不在 Chrome 里。

### 兼容基线

- 新增 `.browserslistrc`，基线定为 Chrome 70 / iOS 12 / Android 7，构建按它降级。依据见 `docs/operations/china-devices.md` 的内核对照表。
- **移除 Tailwind。** 项目一个工具类都没用到，只在用它的 reset，但 Tailwind v4 产出 `@property`、`color-mix()`、`@layer` 和 `oklab()`，这几样在 X5 内核上会让整块样式失效。改为手写 reset，CSS 产物从 10.3 KB 降到 6.9 KB，无一特性超出基线。
- flex 的 `gap` 全部改走 `.row` 和 `.wrap` 两个类，带 `@supports (gap: 1px)` 回退。老内核走负外边距，间距与新内核一致，已实测验证。
- 逻辑属性（`padding-block`、`inset-block`）全部换成物理属性，这类写法构建期不会降级。
- 新增 `LegacyPolyfills`，内联在 head 里补 `Array.prototype.at`、`String.prototype.at`、`Object.hasOwn`、`replaceAll`、`Promise.allSettled`。其中 `.at()` 是必需的，Next.js 客户端路由用它解析 redirect digest，缺了会让答题提交和支付确认的跳转抛错。
- 深色模式做成可降级：`prefers-color-scheme` 要 Chrome 76 / iOS 13，更老的内核会停在浅色令牌上，所以浅色配色独立成立。

### 厂商适配

- 防转码声明 `no-transform` 与 `no-siteapp`，拒绝运营商和百度的移动转码重排页面。
- 双核浏览器 `renderer: webkit`，UC 的 `browsermode`、`nightmode: disable`，微信与 QQ 的 `x5-orientation`。UC 的强制夜间模式此前会把自定义深色配色搅乱。
- `viewport-fit: cover` 加四个 safe-area 变量，处理刘海屏、挖孔屏和底部手势条。不支持 `env()` 的内核回落到 0。
- 找回码输入框固定 16px 字号。iOS 上小于 16px 的输入框聚焦时会强制放大整个页面。
- 中文字体栈按苹方、鸿蒙、小米兰亭、思源、雅黑依次回落。

### 添加到桌面

- 新增 PWA manifest，`display: standalone`。国产厂商浏览器的添加到桌面会用它的名称和图标，没有时会退化成默认地球图案。
- 新增 `npm run build:icons`，纯 Node 手写 PNG 编码，不引入图形库。图案是四条维度倾向条，与产品视觉一致，外圈留 22% 安全边供 Android 自适应图标裁切。

### 搜索引擎

- 站长平台验证码扩展到六家：百度、头条、搜狗、神马、360、华为 Petal，通过环境变量注入，未配置时不输出。
- `applicable-device`、`MobileOptimized`、`HandheldFriendly` 移动适配声明。

### 检查

- 新增 `npm run check:china`，已进 `npm run ship`。检查四类：境外资源引用、源码里超出基线的写法、构建产物里的残留、国内适配必备项。
- 境外域名清单覆盖 Google Fonts、GA、GTM、unpkg、jsdelivr、cdnjs 等，命中即阻断。当前站上零境外资源。

### 文档

- `docs/operations/china-devices.md`：兼容基线的依据、各内核实际情况对照、为此付出的代价、各厂商的具体脾气、添加到桌面、真机验证清单、暂不做快应用的判断。

## 0.2.0 — 2026-09-05

SEO 与 AI 抓取的完整落地，以及微信支付从「链路形态正确」推进到「可以真实接入」。

### SEO

- 新增 16 个静态类型解读页 `/t/<slug>/type/<code>`，每页含字母含义拆解、性格描述、相关类型内链和 FAQ。这是搜索流量的主力入口，也是运营投放的承接页。
- 新增类型索引页 `/t/<slug>/type`，站内链接枢纽，抓取器两跳可遍历全站。
- 介绍页补充理论背景长文和四个维度的完整说明，从「一句话卖点」变成可被索引的内容页。
- 内容包新增 `meta.about`、`meta.updatedAt`、`meta.seo.faq`，校验脚本一并覆盖。
- `sitemap.xml` 与 `robots.txt` 由内容包生成。
- JSON-LD 结构化数据：全站 `WebSite`，介绍页 `Quiz` + `FAQPage`，索引页 `ItemList`，类型页 `Article` + `FAQPage`，均带 `BreadcrumbList`。全部由 Server Component 渲染。
- 用户个人结果页改为 noindex，canonical 指向对应的类型页，避免稀释权重和暴露个人数据。
- FAQ 用原生 `<details>`，折叠状态下内容仍在 HTML 里。
- 新增 `npm run seo:ping`，向百度普通收录和 IndexNow 主动推送，默认预演。
- 站长平台验证码通过环境变量注入。

### AI 友好

- `/llms.txt` 站点索引与 `/llms-full.txt` 全部免费内容的 Markdown 全文，由内容包生成，与页面同源。付费报告不进这两个文件。
- `robots.txt` 按名单显式放行 GPTBot、ClaudeBot、PerplexityBot、Google-Extended、Bytespider 等 AI 抓取器，同时对所有抓取器禁止用户个人结果页。
- llms 文件开头写明引用要求：注明来源、保留链接、说明结果描述偏好而非能力。

### 微信支付

- provider 接口扩展为五件事：预下单、回调验签、主动查单、关单、退款。mock 与 wechat 两个实现都补齐。
- 新增主动查单对账 `src/lib/pay/reconcile.ts`。微信回调会丢也会迟到，前端支付后轮询 `/api/order/<id>/status`，服务端向微信查单把状态补上；H5 支付跳回报告页时也会先对账一次。
- 新增订单状态接口，带鉴权：请求方需持有下单时的 sessionKey 或绑定同一 openId，否则 403。
- 新增 `Refund` 数据模型与 `npm run refund` 人工退款脚本，默认预演，`--yes` 才执行，同一订单不会重复退。
- 新增 `npm run check:pay-rotation`，用商户私钥试签、解析全部平台公钥、检查回调地址，密钥问题在部署前暴露而不是等联调。
- APIv3 密钥长度、平台公钥序列号缺失等错误给出可操作的提示而不是裸异常。
- 支付取消与支付失败在前端区分提示。

### 文档

- `docs/operations/wechat-pay.md` 接入手册：资质与申请、四处后台配置、四类密钥材料的来源与转换、环境变量对照、三种场景的支付链路、八步联调、按阶段分类的错误排查表、对账、上线清单。
- `docs/operations/wechat-pay-key-rotation.md` 轮换手册：三类材料的过期方式、平台公钥追加式轮换、商户证书切换式轮换、APIv3 重置的影响窗口、轮换记录表。
- `docs/operations/seo.md`：页面索引策略、类型页为什么是主力、结构化数据分布、AI 抓取的三件事、国内搜索引擎推送、新增测试时的人工确认项。

### 修复

- 下单接口不再接受客户端传入的 `openId`。此前传入他人 openId 可读到对方订单的找回码，现在只从签名 Cookie 读。
- 微信网页授权接入付费流程。微信内下单缺 openid 时自动走 `snsapi_base` 静默授权，此前这条路径是断的。
- 支付回跳与授权回跳都做了站内地址校验，防开放重定向。
- 关闭 Next.js 自动生成 AGENTS.md。macOS 文件系统大小写不敏感，它会把内容追加进本仓库的 `agents.md`。

### 已知缺口

- 微信支付和网页授权只在 mock 模式下验证过链路形态。真实商户联调需要在有资质的环境里按 `docs/operations/wechat-pay.md` 第九节走一遍。
- 退款回调未实现。退款状态目前以发起时的返回为准，微信标记为受理中的退款需要人工在商户平台确认最终结果。
- 自动对账未实现。上量前每天手工核一次交易账单，方法见接入手册第十一节。
- 微信 JS-SDK 的 `wx.config` 签名接口未实现，站内分享只有复制链接，没有自定义分享卡片标题。
- 微信 JS-SDK 分享缩略图需要位图，`/api/og` 目前只产出 SVG。方案见 `docs/design/share-card.md`。
- 用户自助删除作答的接口未实现，当前走客服邮箱人工处理。

## 0.1.0 — 2026-09-05

第一版：16 型人格测试的完整闭环，以及支撑后续测试的内容包引擎和 agent 工作流。

### 产品与内容

- 「16 型人格测试」内容包 v1.0.0：48 道自建题目，四维度各 12 题，每个维度两极题数 6:6 平衡。
- 16 份类型报告，每份含免费部分（人设标签、三关键词、核心描述、最舒服与最容易累的状态）和付费部分（认知偏好、五条优势、五条盲点及对应动作、职业环境、亲密关系、与其他类型相处、三条成长建议）。
- 类型别名全部原创，不使用任何第三方产品的类型命名。
- 全站不出现 MBTI 商标，题目文本全部自建。

### 引擎

- 内容包规范 `docs/product/test-pack-spec.md`，支持 `dichotomy`、`bands`、`profile` 三种结果模式。
- 计分引擎与具体测试无关，新增测试只写内容包不改代码，`scripts/scoring.test.mts` 用 bands 模式验证了这一点。
- 内容包版本冻结：每次作答记录当时版本号，历史结果不随题库更新变化。

### 功能

- 落地页、答题页（一屏一题、进度条、localStorage 断点续答）、结果页、付费深度报告、找回页、隐私政策、用户协议。
- 匿名会话：完成测试不需要注册，`sessionKey` 走签名 HttpOnly Cookie。
- 支付 provider 抽象，含 mock 与微信支付 v3（JSAPI + H5）实现。抖音内置浏览器无法唤起时返回显式引导，不静默失败。
- 微信网页授权：微信内下单缺 openid 时自动跳 `snsapi_base` 静默授权再回来，只取 openid 不取昵称头像。openId 存签名 HttpOnly Cookie，服务端读，客户端传入一律忽略。
- 支付回调验签、金额核对、事件级幂等，权益以回调为准发放。
- 支付回跳和授权回跳都只允许站内地址，防开放重定向。
- 找回码：付费后换设备可凭码重新打开报告，跨域名有效。
- 结果分享卡 `/api/og` 动态 SVG，不含任何个人信息。

### 工程

- 八个角色 agent 定义与三个流水线命令。
- `check:content` 校验版本一致、题目唯一性、维度与两极平衡、题干长度、类型码覆盖、付费字段完整性。
- `check:compliance` 扫描商标、广告法绝对化用语、心理表达边界、诱导分享、敏感个人信息五组规则，并核对付费承诺与实际交付字段。
- `check:prod-env` 拦截 mock 支付、占位密钥、localhost 地址、以 `NEXT_PUBLIC_` 开头的密钥、无效备案号和客服邮箱。
- 关闭 Next.js 自动生成 AGENTS.md：macOS 文件系统大小写不敏感，它会覆盖本仓库的 `agents.md`。

### 已知缺口

- 微信 JS-SDK 分享缩略图需要位图，目前 `/api/og` 只产出 SVG。方案见 `docs/design/share-card.md`。
- 微信 JS-SDK 的 `wx.config` 签名接口尚未实现，站内分享按钮目前只有复制链接，没有自定义分享卡片标题。
- 用户自助删除作答的接口尚未实现，当前走客服邮箱人工处理。
- 微信支付和网页授权只在 mock 模式下验证过链路形态，真实商户联调需要在有资质的环境里跑一遍。
